import { createStyles } from '@/theme';

export default createStyles(() => ({
  container: { flex: 1 },
  actions: {
    padding: 5,
    textAlign: 'center',
    backgroundColor: '#e1e1e1',
    marginLeft: 10,
    borderRadius: 5,
  },
}))();
