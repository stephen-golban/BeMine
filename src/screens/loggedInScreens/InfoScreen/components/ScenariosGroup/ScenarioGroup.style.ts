import { createStyles } from '@/theme';

export default createStyles((theme) => {
  return {
    subgroup: {
      borderLeftWidth: 2,
      borderRightWidth: 2,
      borderColor: theme.colors.opac_light_gray,
    },
  };
});
